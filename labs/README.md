# Labs

Small, focused experiments designed to answer one engineering question at a time.

Examples: compare chunk sizes, test retrieval strategies, measure tool-call reliability, compare planning approaches, inject failures, measure latency/cost, or reproduce an agent failure.
